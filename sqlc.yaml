version: "2"
sql:
  - engine: "postgresql"
    queries: "./sql/queries.sql"
    schema: "./sql/schema.sql"
    gen:
      go:
        package: "db"
        out: "pkg/db"
        emit_interface: false
        emit_json_tags: true
